#tuples are just like list the only differance is that tuples can't be modified.
# We use tuples only when we don't want anyone to accidently delete some element of lists.
tuple =("first", "second","third")
#instead of square bracket used in list we use parenthesis for tuple.
print(tuple)